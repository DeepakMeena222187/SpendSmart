# In your project root folder
echo '[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200' > netlify.toml

git add netlify.toml
git commit -m "fix: correct netlify build config"
git push